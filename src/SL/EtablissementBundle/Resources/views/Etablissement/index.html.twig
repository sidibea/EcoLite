{% extends "SLHomeBundle::layout.html.twig" %}
{% block title %} Bienvenue Sur ECOLITE {% endblock %}
{% block content %}

<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        Etablissement
        <small>ECOLITE</small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i> Acceuil</a></li>
        <li class="active">Etablissement</li>
    </ol>
</section>

<!-- Main content -->
<section class="content">

    <!-- Main row -->
    <div class="row">
        <!-- Left col -->
        <section class="col-lg-6 connectedSortable">
            <div class="box box-success">
                <div class="box-header">
                    <i class="fa fa-folder"></i>
                    <h3 class="box-title">Informations </h3>
                </div>
                <div class="box-body ">
                    {{ form_start(form) }}

                    {# Les erreurs générales du formulaire. #}
                    {{ form_errors(form) }}

                    <div class="form-group">
                        {# Génération du label. #}
                        {{ form_label(form.type, "Type") }}

                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.type) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.type, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group">
                        {# Génération du label. #}
                        {{ form_label(form.nom, "Nom") }}

                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.nom) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.nom, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group">
                        {# Génération du label. #}
                        {{ form_label(form.pays, "Pays") }}

                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.pays) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.pays, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group">
                        {# Génération du label. #}
                        {{ form_label(form.ville, "Ville") }}

                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.ville) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.ville, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group">
                        {# Génération du label. #}
                        {{ form_label(form.adresse, "Adresse") }}

                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.adresse) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.adresse, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group">
                        {# Génération du label. #}
                        {{ form_label(form.academie, "Academie") }}

                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.academie) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.academie, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group">
                        {# Génération du label. #}
                        {{ form_label(form.telephone, "Telephone") }}

                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.telephone) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.telephone, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>

            </div>

        </section>

    <!-- Left col -->
    <section class="col-lg-6 connectedSortable">
        <div class="box box-success">
            <div class="box-header">
                <i class="fa fa-folder"></i>
                <h3 class="box-title">Logo & Signature </h3>
            </div>
            <div class="box-body ">

                <div class="form-group col-sm-6">
                    <img class="margin" alt="..." src="http://placehold.it/150x100"><br>
                    {# Génération du label. #}
                    {{ form_label(form.logo, "Logo") }}

                    {# Affichage des erreurs pour ce champ précis. #}
                    {{ form_errors(form.logo) }}
                    {# Génération de l'input. #}
                    {{ form_widget(form.logo, {'attr': {'class': 'file'}}) }}
                </div>
                <div class="form-group col-sm-6">
                    <img class="margin" alt="..." src="http://placehold.it/150x100">
                    {# Génération du label. #}
                    {{ form_label(form.cachet, "cachet") }}

                    {# Affichage des erreurs pour ce champ précis. #}
                    {{ form_errors(form.cachet) }}
                    {# Génération de l'input. #}
                    {{ form_widget(form.cachet, {'attr': {'class': 'file'}}) }}
                </div><br>
                <div class="form-group">
                    {# Génération du label. #}
                    {{ form_label(form.email, "Email") }}

                    {# Affichage des erreurs pour ce champ précis. #}
                    {{ form_errors(form.email) }}
                    {# Génération de l'input. #}
                    {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="form-group">
                    {# Génération du label. #}
                    {{ form_label(form.devise, "Devise") }}

                    {# Affichage des erreurs pour ce champ précis. #}
                    {{ form_errors(form.devise) }}
                    {# Génération de l'input. #}
                    {{ form_widget(form.devise, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="form-group">
                    {# Génération du label. #}
                    {{ form_label(form.siteweb, "Site Web") }}

                    {# Affichage des erreurs pour ce champ précis. #}
                    {{ form_errors(form.siteweb) }}
                    {# Génération de l'input. #}
                    {{ form_widget(form.siteweb, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="form-group">
                    {# Génération du label. #}
                    {{ form_label(form.nomDirecteur, "Nom du directeur") }}

                    {# Affichage des erreurs pour ce champ précis. #}
                    {{ form_errors(form.nomDirecteur) }}
                    {# Génération de l'input. #}
                    {{ form_widget(form.nomDirecteur, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="form-group">


                    {# Affichage des erreurs pour ce champ précis. #}
                    {{ form_errors(form.loginDirecteur) }}
                    {# Génération de l'input. #}
                    {{ form_widget(form.loginDirecteur, {'attr': {'class': ''}}) }}
                </div>

            </div>
        </div>
        <div class="">

            {# Pour le bouton, pas de label ni d'erreur, on affiche juste le widget #}
            {{ form_widget(form.Enregistrer, {'attr': {'class': 'btn btn-app pull-right'}}) }}
            {# Génération automatique des champs pas encore écrits.
                 Dans cet exemple, ce serait le champ CSRF (géré automatiquement par Symfony !)
                 et tous les champs cachés (type « hidden »). #}
            {{ form_rest(form) }}

            {# Fermeture de la balise <form> du formulaire HTML #}
            {{ form_end(form) }}
        </div>
    </section>

    </div>


</section>

{% endblock %}