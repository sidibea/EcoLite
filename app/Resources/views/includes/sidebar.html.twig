<!-- sidebar: style can be found in sidebar.less -->
<section class="sidebar">
    <!-- Sidebar user panel -->
    <div class="user-panel">
        <div class="pull-left image">
            <img src="{{ asset('favicon.png') }}" class="img-circle" alt="User Image" />
        </div>
        <div class="pull-left info">
            <p>{ app.user.firstname } { app.user.lastname }</p>

            <a href="#"><i class="fa fa-circle text-success"></i> Online</a>
        </div>
    </div>

    <!-- sidebar menu: : style can be found in sidebar.less -->
    <ul class="sidebar-menu">
        <li class="header">MAIN NAVIGATION</li>
        <li class="treeview">
            <a href="{{ path('sl_index_homepage') }}">
                <i class="fa fa-dashboard"></i> <span>TABLEAU DE BORD</span>
            </a>
        </li>
        {% if is_granted('ROLE_ADMINISTRATION') %}
        <li class="treeview">
            <a href="{{ path('sl_configuration_etablissement') }}">
                <i class="fa fa-institution"></i>
                <span>ETABLISSEMENT</span>
            </a>
        </li>
        {% endif %}

        {% if is_granted('ROLE_ADMINISTRATION') %}
            <li>
                <a href="#">
                    <i class="fa fa-key"></i>
                    <span>ADMINISTRATION</span>
                </a>
                <ul class="treeview-menu">
                    <li><a href="{{ path('sl_users_list') }}"><i class="fa fa-circle-o"></i>Employes</a></li>

                </ul>
            </li>
        {% endif %}


    </ul>
</section>
<!-- /.sidebar -->